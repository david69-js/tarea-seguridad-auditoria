# Entorno
.env
.env.local

# Dependencias
/vendor/
node_modules/
package-lock.json
!frontend/package-lock.json

# Build del frontend (se genera con "npm run build" o en el Dockerfile)
/public/app/

# Imágenes subidas por usuarios (se conserva la carpeta con .gitkeep)
/public/uploads/*
!/public/uploads/.gitkeep
!/public/uploads/.htaccess

# Sistema / editores
.DS_Store
Thumbs.db
*.log
.idea/
.vscode/
*.swp

# Documento original de la tarea
Tarea_POS_Web.docx

# Archivos temporales de Word mientras un documento está abierto
~$*
